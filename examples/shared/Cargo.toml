[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = {
  version = "0.19.0-rc.1",
  default-features = false,
  features = ["debug"]
}
bevy-sequential-actions = { path = "../../" }
